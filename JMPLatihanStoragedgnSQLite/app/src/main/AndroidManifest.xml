<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.coedotz.jmplatihanstoragedgnsqlite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JMPLatihanStorageDgnSQLite">
        <activity
            android:name=".java.ExternalStorageJava"
            android:exported="false" />
        <activity
            android:name=".kotlin.ExternalStorageKotlin"
            android:exported="false" />
        <activity
            android:name=".kotlin.InternalStorageKotlin"
            android:exported="false" />
        <activity
            android:name=".java.InternalStorageJava"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".kotlin.MainActivity" -->
        <!-- android:exported="true"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
        <!-- </intent-filter> -->
        <!-- </activity> -->
        <!-- JAVA -->
        <activity
            android:name=".java.MainActivityJava"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>